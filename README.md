# **Quick Setup (3 Commands)**
```bash
# Create Project
npm create vite@latest moekyawaung-react -- --template react
cd moekyawaung-react

# Install Dependencies
npm install lucide-react framer-motion tailwindcss postcss autoprefixer clsx tailwind-merge @headlessui/react
